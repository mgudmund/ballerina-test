name: CI

on: [push]

jobs:
  build:
    container:
        image: ballerina/ballerina:latest
        volumes:
        - my_docker_volume:/volume_mount
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: ls -la 
    - name: Build
      run: sudo ballerina build *.bal
    - name: Run a multi-line script
      run: ls -la
