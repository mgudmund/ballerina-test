name: CI

on: [push]

jobs:
  build:
    
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: pwd 
    - name: Build
      run:  ls
    - name: Run a multi-line script
      run: ls -la
    - uses: docker://ballerina/ballerina:latest
      run: pwd 
  
